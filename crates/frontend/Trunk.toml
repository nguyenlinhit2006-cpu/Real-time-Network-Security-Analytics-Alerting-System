[build]
target = "index.html"
dist = "dist"

[watch]
watch = ["src"]

[serve]
address = "0.0.0.0"
port = 3000
open = false

[[proxy]]
rewrite = "/api"
backend = "http://127.0.0.1:8080/api"

[[proxy]]
rewrite = "/ws"
backend = "ws://127.0.0.1:8080/ws"
ws = true
